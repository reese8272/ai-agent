'''
Our global variables, such as the max chars we will read from a file,
the working directory we wish our LLM to work from,
and the max iterations we will let our model run for before we stop the process.
'''

MAX_CHARS = 10000
WORKING_DIR = "./calculator"
MAX_ITERS = 20